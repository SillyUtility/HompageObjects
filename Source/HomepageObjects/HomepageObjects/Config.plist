/* Default Config.plist */
{
    // basic site variables
    title = "site title";
    baseURL = "https://base.tld";

    // directories
    siteDirectory = ".";
    contentDirectory = content;
    layoutDirectory = layouts;
    templateDirectory = templates;
    buildDirectory = build;
}
